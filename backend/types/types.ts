export type TBook = {
    title: string;
    author: string;
    ID: string;
    rating: number;

};

export type TUser = {
    username: string;
    email: string;
    password: string;
    role: string;
};